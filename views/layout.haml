!!!
%html{:lang => "en"}
  %head
    = partial 'shared/header'

  %body.body-custom
    - if current_user
      = partial 'shared/nav-menu'

    .container-fluid
      .row-fluid
        .span12

          - if flash?
            = partial 'shared/flash'
          = content
