%img{:src => user_avatar_path(current_user)}
"You are logged in as #{current_user.email}"

%h2= "#{space.name.capitalize}'s applications"


#apps-list
  %ul
    - apps.each do |app|
      %li= app.name.capitalize
